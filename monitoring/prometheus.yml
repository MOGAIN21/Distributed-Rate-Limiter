global:
  scrape_interval: "15s"
  evaluation_interval: "15s"
  
scrape_configs:
  - job_name: 'ratelimiter'
    static_configs:
      - targets: ['ratelimiter-server:8080']
        labels:
          service: 'rate-limiter'