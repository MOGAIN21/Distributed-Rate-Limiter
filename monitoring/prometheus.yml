global:
  scrape_interval: "1m"
  evaluation_interval: 
  
scrape_configs:
  - job_name: 'ratelimiter'
    static_configs:
      - targets: ['ratelimiter-server:8080']
        labels:
          service: 'rate-limiter'